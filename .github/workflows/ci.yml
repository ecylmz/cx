name: CI

on:
  push:
    branches: [main, initial-cx-import]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - run: go test ./...
      - run: go vet ./...
      - run: go build ./...
      - name: Validate installer syntax
        run: sh -n install.sh
      - name: Installer smoke test
        shell: bash
        run: |
          set -euo pipefail
          CX_INSTALL_DIR="$RUNNER_TEMP/bin" ./install.sh
          "$RUNNER_TEMP/bin/cx" version
